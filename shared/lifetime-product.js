export const LIFETIME_AMOUNT = 599;
export const LIFETIME_CURRENCY = "usd";
export const LIFETIME_PRICE_LABEL =
  `$${(LIFETIME_AMOUNT / 100).toFixed(2)}`;
export const LIFETIME_PRICE_ONCE = `${LIFETIME_PRICE_LABEL} once`;
